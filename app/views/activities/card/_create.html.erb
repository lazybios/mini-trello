在列表 <%= link_to activity.trackable.list.title, board_lists_path(activity.trackable.list.board) %> 中创建了卡片
<%= link_to activity.trackable.title, board_lists_path(activity.trackable.list.board) %>
at <%= time_ago_in_words(activity.created_at) %>
